---
phase: 7
plan: 5
wave: 3
depends_on: ["3", "4"]
files_modified: ["db/clickhouse/002_optimize_indexes.sql", "docs/DEPLOYMENT.md", "docs/CONFIGURATION.md"]
autonomous: true

must_haves:
  truths:
    - "ClickHouse tables use bloom filters for UUID lookups"
  artifacts:
    - "db/clickhouse/002_optimize_indexes.sql"
    - "docs/DEPLOYMENT.md"
    - "docs/CONFIGURATION.md"
---

# Plan 7.5: ClickHouse Optimization & Deployment Documentation

<objective>
Optimize ClickHouse for production-scale queries and create deployment documentation.

Purpose: Operational readiness for v1.0.
Output: ClickHouse Skip-Index migration, Deployment markdown, Configuration reference.
</objective>

<context>
Load for context:
- .gsd/ROADMAP.md
- internal/config/config.go
</context>

<tasks>

<task type="auto">
  <name>ClickHouse partitioning and indexing optimization</name>
  <files>
    db/clickhouse/002_optimize_indexes.sql
  </files>
  <action>
    1. Construct ALTER queries to add SKIP indexes on `country_code`, `campaign_id`, `is_bot` and `click_token`.
    2. Use `bloom_filter` for UUIDs and `set(0)` for low cardinality items.
    3. Include `MATERIALIZE INDEX` queries immediately following addition.
    AVOID: Attempting to `ALTER ORDER BY` or `ALTER PARTITION BY` on existing tables without recreation.
  </action>
  <verify>test -f db/clickhouse/002_optimize_indexes.sql && echo "Migration file exists"</verify>
  <done>Indexes configured to dramatically reduce analytic query scans</done>
</task>

<task type="auto">
  <name>Create deployment documentation</name>
  <files>
    docs/DEPLOYMENT.md
    docs/CONFIGURATION.md
  </files>
  <action>
    1. `DEPLOYMENT.md`: cover Docker Compose, Binary deployments, Systemd Unit, health checks, monitoring configurations.
    2. Include a concrete "Production Checklist".
    3. `CONFIGURATION.md`: outline the full YAML spec with defaults.
    AVOID: Using placeholders like `[INSERT COMMAND HERE]`. Supply realistic configurations.
  </action>
  <verify>test -f docs/DEPLOYMENT.md && echo "Docs created"</verify>
  <done>User can read deployment documentation to fully launch the system</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] SQL migration syntax is valid syntax
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
