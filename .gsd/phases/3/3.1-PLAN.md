---
phase: 3
plan: 1
wave: 1
---

# Plan 3.1: Admin API Scaffolding

## Objective
Establish the foundation for the Administrative API, including authentication middleware and unified JSON response handling.

## Context
- .gsd/SPEC.md
- internal/server/server.go
- internal/server/routes.go

## Tasks

<task type="auto">
  <name>Implement Admin Middleware and Helpers</name>
  <files>internal/server/admin.go</files>
  <action>
    Create internal/server/admin.go.
    Implement:
    - adminAuth middleware that checks for 'X-API-Key' header against config.
    - jsonResponse helper for consistent API outputs.
    - errorResponse helper for consistent error mapping.
  </action>
  <verify>test -f internal/server/admin.go</verify>
  <done>Middleware and helpers are defined and compile.</done>
</task>

<task type="auto">
  <name>Mount Admin Route Group</name>
  <files>internal/server/routes.go</files>
  <action>
    Update internal/server/routes.go to:
    - Define a chi.Router group for /api/v1/admin.
    - Apply the adminAuth middleware to this group.
    - Add a dummy GET /ping endpoint within the group for verification.
  </action>
  <verify>grep -q "/api/v1/admin" internal/server/routes.go</verify>
  <done>Admin route group is mounted with authentication.</done>
</task>

## Success Criteria
- [ ] /api/v1/admin/ping returns 200 OK with a valid API key.
- [ ] /api/v1/admin/ping returns 401 Unauthorized without a valid API key.
