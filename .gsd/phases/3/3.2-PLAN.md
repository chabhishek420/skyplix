---
phase: 3
plan: 2
wave: 2
---

# Plan 3.2: Campaign CRUD

## Objective
Implement full RESTful CRUD operations for Campaigns with automatic Valkey cache synchronization.

## Context
- .gsd/SPEC.md
- internal/model/models.go
- internal/cache/cache.go
- internal/server/admin.go

## Tasks

<task type="auto">
  <name>Implement Campaign CRUD Handlers</name>
  <files>internal/server/admin_campaign.go</files>
  <action>
    Create internal/server/admin_campaign.go.
    Implement:
    - handleAdminListCampaigns (GET /campaigns)
    - handleAdminCreateCampaign (POST /campaigns)
    - handleAdminGetCampaign (GET /campaigns/{id})
    - handleAdminUpdateCampaign (PUT /campaigns/{id})
    - handleAdminDeleteCampaign (DELETE /campaigns/{id})
    Use pgxpool for direct DB access.
  </action>
  <verify>test -f internal/server/admin_campaign.go</verify>
  <done>All CRUD handlers are implemented and integrated into the admin router.</done>
</task>

<task type="auto">
  <name>Integrate Cache Warmup Triggers</name>
  <files>internal/server/admin_campaign.go</files>
  <action>
    Modify Create/Update/Delete handlers to call s.cache.Warmup(ctx) or a more granular s.cache.WarmupCampaign(ctx, id) after successful DB operations.
    Note: Since s.cache.Warmup currently reloads everything, it is acceptable for v1, but consider adding s.cache.WarmupCampaign for efficiency.
  </action>
  <verify>grep -q "s.cache.Warmup" internal/server/admin_campaign.go</verify>
  <done>Cache is synchronized upon campaign mutation.</done>
</task>

## Success Criteria
- [ ] Campaign can be created via API and immediately routes traffic.
- [ ] Campaign updates (e.g. alias change) are reflected in routing within 100ms.
- [ ] Integration tests in test/admin_campaign_test.go pass.
