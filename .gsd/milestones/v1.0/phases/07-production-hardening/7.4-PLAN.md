---
phase: 7
plan: 4
wave: 2
depends_on: ["1"]
files_modified: ["test/load/click_pipeline.js", "test/load/README.md", "test/bench/pipeline_bench_test.go"]
autonomous: true

must_haves:
  truths:
    - "A reproducible k6 benchmark tests L1 click latency"
  artifacts:
    - "test/load/click_pipeline.js"
---

# Plan 7.4: Load Testing & Performance Benchmarks

<objective>
Create reproducible load tests proving the <5ms p99 latency requirement.

Purpose: Verification of latency compliance.
Output: k6 load test scripts and Go bench stubs.
</objective>

<context>
Load for context:
- .gsd/SPEC.md
- cmd/zai-tds/main.go
</context>

<tasks>

<task type="auto">
  <name>Create k6 load test scripts</name>
  <files>
    test/load/click_pipeline.js
    test/load/README.md
  </files>
  <action>
    1. Write `click_pipeline.js` mapping sustained (1K RPS) and spike (up to 5K RPS) behavior.
    2. Add thresholds: `p(99)<5` for sustained, `p(99)<10` for spikes.
    3. Exclude following redirects (since we only care about click ingest API latency).
    4. Draft instructions in `test/load/README.md`.
    AVOID: Relying on seeded campaign data. Use robust fallbacks (404/defaults) to easily assess base pipeline overhead.
  </action>
  <verify>k6 --version || echo "Ensure K6 script follows JS formatting"</verify>
  <done>Load test script created and documented</done>
</task>

<task type="auto">
  <name>Create Go benchmark stub for hot path</name>
  <files>
    test/bench/pipeline_bench_test.go
  </files>
  <action>
    1. Write an empty `BenchmarkClickEndpoint` stub that returns `b.Skip()`.
    2. Explain in comments that k6 must be used for full integration tests.
    AVOID: Mocking the entire pipeline just for a Go benchmark.
  </action>
  <verify>go test -v ./test/bench/... || true</verify>
  <done>Stub acts as signpost towards k6 tests</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] K6 script is syntactically valid
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
