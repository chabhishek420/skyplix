---
phase: 7
plan: 7
wave: 3
depends_on: ["2"]
files_modified: ["scripts/migrate_keitaro.go", "go.mod"]
autonomous: true

must_haves:
  truths:
    - "A standalone migration tool can transform Keitaro MySQL exports into ZAI TDS schemas"
  artifacts:
    - "scripts/migrate_keitaro.go"
---

# Plan 7.7: Keitaro Data Migration Script

<objective>
Write a migration script capable of reading Keitaro MySQL structures and importing fundamental items to ZAI PostgreSQL.

Purpose: Required functionality outlined in ROADMAP for operational transfer to ZAI TDS.
Output: A CLI tool within `scripts/`
</objective>

<context>
Load for context:
- .gsd/ROADMAP.md
- internal/model/models.go
</context>

<tasks>

<task type="auto">
  <name>Implement MySQL to PostgreSQL Migrator</name>
  <files>
    scripts/migrate_keitaro.go
  </files>
  <action>
    1. Write a standalone Go script `scripts/migrate_keitaro.go` that connects to a Keitaro MySQL database and a ZAI Postgres database directly.
    2. Support migrating `campaigns` and `streams`. Keitaro tables: `keitaro_campaigns`, `keitaro_streams`.
    3. Generate new ZAI-oriented UUIDs for migrated records.
    4. If dependencies (`github.com/go-sql-driver/mysql`) are needed, advise on `go get`.
    AVOID: Complex historical log migration (like raw click tables). Focus exclusively on core configuration items (Campaigns, Streams).
  </action>
  <verify>go build scripts/migrate_keitaro.go && echo "Migration script compiles"</verify>
  <done>Migration script supports basic bulk insertions from legacy standard</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] `migrate_keitaro.go` compiles correctly
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
