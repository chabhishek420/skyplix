---
phase: 7
plan: 6
wave: 3
depends_on: ["1", "2"]
files_modified: ["internal/pipeline/stage/22_checks.go", "scripts/sync_claude.sh"]
autonomous: true

must_haves:
  truths:
    - "PostProcessChecks enforces loop limits returning 500 when exceeded"
    - "A hygiene script facilitates easy CLAUDE.md resyncing"
  artifacts:
    - "scripts/sync_claude.sh"
---

# Plan 7.6: Audit Debt Closure

<objective>
Resolve legacy v1.0 audit debt: enforce Stage 22 loop limit logic, supply CLAUDE.md sync script.

Purpose: Adherence to specification details that were logged but not enforced.
Output: Strict pipeline checking, script tools.
</objective>

<context>
Load for context:
- .gsd/ROADMAP.md
- internal/pipeline/stage/22_checks.go
- CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Enforce Stage 22 Loop Limit Check</name>
  <files>
    internal/pipeline/stage/22_checks.go
  </files>
  <action>
    1. Update `PostProcessChecks` (or an equivalent Stage 22). If `CheckSendingToAnotherCampaign` logs a dangerous state or if loop-depth goes beyond bounds, we must enforce it actively.
    2. Convert any warning log inside Stage 22 into an actual abort rule (e.g. `payload.AbortCode = http.StatusLoopDetected`).
    AVOID: Leaving the logic as a silent log. Stop the loop.
  </action>
  <verify>go build ./internal/pipeline/stage/... && echo "Build passes"</verify>
  <done>Redirection logic stops infinite loop behavior properly</done>
</task>

<task type="auto">
  <name>Create CLAUDE.md sync automation</name>
  <files>
    scripts/sync_claude.sh
  </files>
  <action>
    1. Write a shell script `scripts/sync_claude.sh` that regenerates or organizes `CLAUDE.md` based on latest codebase changes and GSD `.gsd/STATE.md`.
    2. Make the file executable (`chmod +x`).
    AVOID: Relying on manual updates of CLAUDE.md. Outline the clear sed/awk or file concatenation needed. (E.g. extracting `Phase 7` updates).
  </action>
  <verify>test -f scripts/sync_claude.sh && echo "Sync script created"</verify>
  <done>Sync script is executable and available</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] `scripts/sync_claude.sh` has `+x` permissions
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
