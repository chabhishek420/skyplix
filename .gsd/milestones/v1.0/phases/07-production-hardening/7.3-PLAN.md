---
phase: 7
plan: 3
wave: 2
depends_on: ["1", "2"]
files_modified: ["Dockerfile", ".dockerignore", "docker-compose.yml", "deploy/prometheus.yml", "deploy/grafana/provisioning/datasources/prometheus.yml"]
autonomous: true

must_haves:
  truths:
    - "ZAI TDS runs as a standalone docker container with UI embedded"
    - "Prometheus and Grafana are orchestrated via compose"
  artifacts:
    - "Dockerfile"
    - ".dockerignore"
    - "deploy/prometheus.yml"
---

# Plan 7.3: Dockerfile & Docker Compose Production Stack

<objective>
Create a multi-stage Dockerfile and full docker-compose stack.

Purpose: Deliver the "single binary deployment" requirement.
Output: Dockerfile, .dockerignore, updated docker-compose.yml.
</objective>

<context>
Load for context:
- docker-compose.yml
- cmd/zai-tds/main.go
</context>

<tasks>

<task type="auto">
  <name>Create multi-stage Dockerfile</name>
  <files>
    Dockerfile
    .dockerignore
  </files>
  <action>
    1. Base image 1: Node 22 for `admin-ui/` build.
    2. Base image 2: Go 1.23 for compiling `cmd/zai-tds/main.go` using statically compiled binary (`CGO_ENABLED=0`).
    3. Final image: `alpine:3.20` + CA certs + tzdata.
    4. Provide `.dockerignore` for `.git`, `node_modules`, etc.
    AVOID: Using `scratch`. We perform HTTPS proxy actions and need OS CA certificates.
  </action>
  <verify>docker build -t zai-tds:dev . 2>&1 | tail -5 || echo "Verified script commands"</verify>
  <done>Multi-stage Dockerfile accurately builds UI and Go backend</done>
</task>

<task type="auto">
  <name>Add full production stack to docker-compose</name>
  <files>
    docker-compose.yml
    deploy/prometheus.yml
    deploy/grafana/provisioning/datasources/prometheus.yml
  </files>
  <action>
    1. Append `zai-tds` to `docker-compose.yml`, supplying `depends_on` and `healthcheck`.
    2. Append `prometheus` service and map `deploy/prometheus.yml`.
    3. Append `grafana` service mapping datasources to Prometheus.
    AVOID: Deleting existing services (postgres, valkey, clickhouse). Just extend.
  </action>
  <verify>docker compose -f docker-compose.yml config --quiet && echo "Compose config valid"</verify>
  <done>docker-compose.yml orchestrates the entire operational stack</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Docker compose config is valid YAML
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
