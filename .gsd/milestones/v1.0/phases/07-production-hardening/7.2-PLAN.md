---
phase: 7
plan: 2
wave: 1
depends_on: []
files_modified: ["internal/server/routes.go", "internal/config/config.go", "internal/server/server.go"]
autonomous: true

must_haves:
  truths:
    - "/api/v1/health returns detailed backend status for connected services"
    - "Application refuses to start with invalid configurations"
  artifacts: []
---

# Plan 7.2: Deep Health Checks & Config Validation

<objective>
Upgrade `/api/v1/health` endpoint to a deep probe and add startup config validation with clear error messages.

Purpose: Enables Kubernetes/Docker liveness and readiness probes.
Output: Hardened `/api/v1/health` and `/api/v1/ready` endpoints, strict config validation.
</objective>

<context>
Load for context:
- internal/server/routes.go
- internal/server/server.go
- internal/config/config.go
</context>

<tasks>

<task type="auto">
  <name>Implement deep health check endpoint</name>
  <files>
    internal/server/routes.go
    internal/server/server.go
  </files>
  <action>
    1. Add `startTime time.Time` to `Server` struct. Set in `New()`.
    2. Rewrite `handleHealth()` to aggressively ping PostgreSQL, Valkey, and ClickHouse.
    3. If any fail, include error in JSON but return 200/503 depending on liveness vs readiness. Give health (liveness) 200 if server responds, but degraded.
    4. Add a `/api/v1/ready` endpoint that returns 503 if any critical backend is down.
    AVOID: Failing the health check if ClickHouse is absent, since CH is an optional analytics dependency.
  </action>
  <verify>go build ./... && echo "Build passes"</verify>
  <done>Health pings all backends, /ready enforces hard dependency availability</done>
</task>

<task type="auto">
  <name>Harden config validation</name>
  <files>
    internal/config/config.go
    cmd/zai-tds/main.go
  </files>
  <action>
    1. Add `Warnings() []string` to `Config` struct. Return non-fatal config issues like missing salt, omitted CH/Geo.
    2. In `main.go`, invoke `Warnings()` on startup and `logger.Warn()` them.
    3. Validate `server.port` (1-65535), `rate_limit_per_ip` (>= 1), `rate_limit_window` (>= 1s).
    AVOID: Exiting the program for warnings. Only `validate()` should throw fatal errors.
  </action>
  <verify>go build ./... && echo "Build passes"</verify>
  <done>Config struct validates key bounds and emits warnings gracefully</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Code compiles
- [ ] Warning logic is wired into startup
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
