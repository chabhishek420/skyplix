---
phase: 6
plan: 6
wave: 3
depends_on: [4, 5]
files_modified:
  - admin-ui/src/pages/logs/clicks.tsx
  - admin-ui/src/pages/logs/conversions.tsx
  - admin-ui/src/styles/globals.css
autonomous: true

must_haves:
  truths:
    - "Raw click and conversion logs are viewable via server-side paginated tables"
    - "The application feels extremely premium with deep visual polish"
---

# Plan 6.6: Logs Viewers & App Polish

<objective>
Finalize the UI by building the critical system logs viewers (Clicks, Postbacks, Conversions) and applying a layer of intensive visual polish across the entire application.

Purpose: Operators need access to raw click/postback logs to debug traffic and attribution issues. The UI must fulfill the stringent aesthetic constraints (WOW factor).
Output: Paged log viewers and globally applied CSS/Framer-motion micro-animations.
</objective>

<context>
Load for context:
- admin-ui/src/styles/globals.css
- admin-ui/src/components/layout/main-layout.tsx
- internal/admin/handler/reports.go
</context>

<tasks>

<task type="auto">
  <name>Raw Logs Viewers</name>
  <files>admin-ui/src/pages/logs/clicks.tsx, admin-ui/src/pages/logs/conversions.tsx, admin-ui/src/App.tsx</files>
  <action>
    Implement grids for `Clicks` and `Conversions` logs.
    Since logs can be massive, implement server-side pagination and querying using React Table configured for external data fetching.
    Display granular details like `SubID`, `IP`, `UserAgent`, `Device`, `Status`, `Payout`.
    AVOID: Fetching all logs at once causing browser crashes. Implement strict pagination.
  </action>
  <verify>npm run lint cleanly passes</verify>
  <done>Log viewers handle large datasets smoothly with pagination controls</done>
</task>

<task type="auto">
  <name>Aesthetic Polish & Transitions</name>
  <files>admin-ui/src/styles/globals.css, admin-ui/src/components/layout/main-layout.tsx</files>
  <action>
    Inject "wow factor" into the global CSS. Implement a curated dark mode palette extending Tailwind.
    Add subtle glassmorphism (backdrop-blur) properties to headers and modals.
    Use framer-motion or CSS transitions to animate page mounting (fade + slight slide up), hover effects on table rows, and interactive feedback for form submissions (toast notifications).
    AVOID: Overwhelming animations that feel slow. Keep durations short (150ms - 300ms).
  </action>
  <verify>npm run build compiles optimized CSS</verify>
  <done>Application achieves a highly responsive, premium state-of-the-art feel as mandated by the instructions.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Logs viewer cleanly displays data without locking the browser.
- [ ] Application aesthetics match top-tier modern web apps.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
