---
phase: 6
plan: 8
wave: 1
---

# Plan 6.8: Data-First Layout & Entity Redesign

## Objective
Implement high-density UI patterns and "Clean White" card surfaces for all core entities (Campaigns, Streams, Offers, Logs). Focus on data visibility and professional contrast levels.

## Context
- `admin-ui/src/components/DataTable.tsx` — Standard table component
- `admin-ui/src/pages/campaigns/` — Campaign-specific views
- `admin-ui/src/pages/logs/` — Log views (Clicks/Conversions)
- `admin-ui/src/index.css` — High-density typography rules

## Tasks

<task type="auto">
  <name>High-Density Table Component</name>
  <files>
    <file>admin-ui/src/components/DataTable.tsx</file>
  </files>
  <action>
    Modify the main `DataTable` component to support the "Original" density:
    - Reduce default cell padding (`py-2` or `h-10`).
    - Standardize font size to `13px` or `14px` for data rows.
    - Implement subtle zebra-striping using `bg-slate-50/50` for even/odd rows.
    - Ensure headers are sticky and have a clean bottom border without heavy background colors.
    - Ensure positive metrics (ROI%, Profit) are highlighted with Emerald-600 text.
  </action>
  <verify>Visual check of table source code for padding and font-size changes.</verify>
  <done>Data tables fit more rows on screen with clear readability.</done>
</task>

<task type="auto">
  <name>Redesign Entity Management Pages</name>
  <files>
    <file>admin-ui/src/pages/campaigns/CampaignList.tsx</file>
    <file>admin-ui/src/pages/offers/OfferList.tsx</file>
    <file>admin-ui/src/pages/landings/LandingList.tsx</file>
  </files>
  <action>
    Update list-view pages:
    - Switch component containers to pure white backgrounds.
    - Replace heavy shadows with the new subtle Slate shadow.
    - Add clean "Create" buttons (Blue-600) and compact row actions.
    - Ensure Status Badges use the "Clean White" style (e.g. `bg-emerald-100 text-emerald-800` for active).
  </action>
  <verify>Visual check of component wrappers and button styles.</verify>
  <done>Entity management pages follow the "Clean White" design language.</done>
</task>

## Success Criteria
- [ ] Tables fit ~15+ rows per screen (on standard 1080p).
- [ ] ROI and Profit columns automatically highlight green/red.
- [ ] Status badges (ACTIVE/PAUSED) are compact and high-contrast.
