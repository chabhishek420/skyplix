---
phase: 6
plan: 1
wave: 1
depends_on: []
files_modified:
  - admin-ui/package.json
  - admin-ui/vite.config.ts
  - internal/server/embed.go
  - internal/server/server.go
autonomous: true

must_haves:
  truths:
    - "Vite+React 19 project is scaffolded in admin-ui/"
    - "Go server correctly serves the production build via //go:embed"
  artifacts:
    - "admin-ui/index.html"
    - "internal/server/embed.go"
---

# Plan 6.1: Vite Scaffold and Go Embed

<objective>
Scaffold the React SPA using Vite and ensure the Go binary smoothly serves the `admin-ui/dist` files using the `//go:embed` directive. 

Purpose: To establish the foundational architecture closing the "Single Binary Deployment" gap, ensuring the UI is built and served natively.
Output: A React app that compiles into dist/, and Go handlers routing `/*` (excluding `/api/*` and `/postback/*`) to the embedded static files.
</objective>

<context>
Load for context:
- .gsd/ARCHITECTURE.md
- cmd/zai-tds/main.go
- internal/server/server.go
</context>

<tasks>

<task type="auto">
  <name>Scaffold React Project</name>
  <files>admin-ui/package.json, admin-ui/vite.config.ts</files>
  <action>
    From within the admin-ui directory, initialize a Vite + React (TypeScript) project. 
    Install TailwindCSS v4 with Vite plugin, and install generic shadcn/ui dependencies. 
    Update `vite.config.ts` to proxy `/api` and `/postback` to `http://localhost:8105` for seamless local dev.
    AVOID: Modifying anything outside `admin-ui/` in this step. Use `pnpm` or `npm`. Keep generic Tailwind standard since user didn't specify beyond "vanilla CSS + Tailwind if requested". We will use Tailwind as it pairs well with shadcn/ui.
  </action>
  <verify>cd admin-ui && npm run build generates dist/ folder</verify>
  <done>Vite build succeeds and outputs to admin-ui/dist</done>
</task>

<task type="auto">
  <name>Implement Go SPA Embedding</name>
  <files>internal/server/embed.go, internal/server/server.go, internal/server/routes.go</files>
  <action>
    Create `internal/server/embed.go` declaring a `//go:embed all:../../admin-ui/dist` variable for `embed.FS`.
    Update `internal/server/routes.go` to handle SPA routing: any GET request not matching `/api`, `/postback`, or `/health` should fall back to serving the embedded `index.html`.
    AVOID: Registering the static handler before API routes, or else the UI will shadow the API endpoints. AVOID fatal errors if `dist/` is empty during tests (use fallbacks or conditional mounting).
  </action>
  <verify>go build -o tmp_bin cmd/zai-tds/main.go (must compile without embed errors)</verify>
  <done>Go binary builds successfully and serves JS/CSS/HTML assets on root</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Vite dev server proxies API calls correctly
- [ ] Go binary serves the production build
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
