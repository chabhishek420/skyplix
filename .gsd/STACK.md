# Technology Stack

> Auto-generated by /map on 2026-04-02
> Implementation Status: **PRE-PHASE 1 — planned, not yet installed**
> Decisions documented in `.gsd/DECISIONS.md` and `.gsd/RESEARCH.md`

---

## Runtime

| Technology | Version | Purpose |
|------------|---------|---------|
| **Go** | 1.23+ | Core runtime — single binary, goroutine concurrency |
| **Node.js** | 22 LTS | Admin UI build toolchain only (not shipped in production) |

---

## Go Backend Dependencies

### HTTP & Routing

| Package | Version | Purpose |
|---------|---------|---------|
| `github.com/go-chi/chi/v5` | v5.x | HTTP router — stdlib-compatible, idiomatic, zero magic |

> **ADR-003**: Chosen over Fiber (fasthttp) because Chi is fully `net/http` compatible. The TDS bottleneck is GeoIP + Valkey I/O (~2–6ms), not routing (~50µs). Fiber's ~20% routing speedup is statistically invisible.

### Database

| Package | Version | Purpose |
|---------|---------|---------|
| `github.com/jackc/pgx/v5` | v5.x | PostgreSQL native driver + pgxpool connection pool |
| `github.com/sqlc-dev/sqlc` | latest | Build-time: SQL → type-safe Go code generation (zero reflection) |
| `github.com/Masterminds/squirrel` | v2.x | Dynamic query builder for admin list filters (pagination, sorting) |
| `github.com/golang-migrate/migrate/v4` | v4.x | Database migrations — embedded in binary, auto-run at startup |

> **ADR-004**: `sqlc + pgx` chosen over GORM. sqlc generates compile-time safe Go from plain SQL — zero reflection on hot path. Squirrel used only for dynamic `WHERE` clauses in admin list endpoints.

### Analytics

| Package | Version | Purpose |
|---------|---------|---------|
| `github.com/ClickHouse/clickhouse-go/v2` | v2.x | ClickHouse driver (backed by ch-go, native protocol) |

### Cache / Queue

| Package | Version | Purpose |
|---------|---------|---------|
| `github.com/redis/go-redis/v9` | v9.x | Valkey/Redis client (RESP3 protocol compatible) |

> **ADR-002**: Valkey 8 chosen over Redis OSS for BSD open-source license. Drop-in compatible — same client library.

### GeoIP & Device Detection

| Package | Version | Purpose |
|---------|---------|---------|
| `github.com/oschwald/geoip2-golang` | latest | MaxMind .mmdb reader — country, city, ASN lookups |
| `github.com/robicode/device-detector` | latest | UA parsing — full Matomo parity (device model, brand, browser, OS). Requires PCRE. |
| *OR* `github.com/mileusna/useragent` | latest | Lightweight UA parser — no PCRE, but missing DeviceModel/Brand fields |

> **Decision pending (Phase 1):** Evaluate `robicode/device-detector` first for full Keitaro
> parity on 6 device stream filters. If PCRE dependency conflicts with single-binary
> deployment, fall back to `mileusna/useragent` with custom enrichment.

### Observability

| Package | Version | Purpose |
|---------|---------|---------|
| `go.uber.org/zap` | v1.x | Structured JSON logging (~10ns per call, zero allocation) |
| `github.com/prometheus/client_golang` | v2.x | Prometheus metrics — `/metrics` endpoint |

### Utilities

| Package | Version | Purpose |
|---------|---------|---------|
| `gopkg.in/yaml.v3` | v3.x | Config file parsing (`config.yaml`) |
| `github.com/google/uuid` | latest | UUID v4 generation for entity IDs |
| `crypto/rand` | stdlib | Click token generation (cryptographically secure) |

### Testing

| Package | Version | Purpose |
|---------|---------|---------|
| `github.com/stretchr/testify` | v1.x | Assertions + mocking |
| `net/http/httptest` | stdlib | HTTP handler testing |

---

## Frontend Dependencies (Admin UI)

Built with Vite; compiled static bundle embedded in the Go binary via `//go:embed admin-ui/dist`.

### Production

| Package | Version | Purpose |
|---------|---------|---------|
| `react` | 19.x | UI component framework |
| `react-dom` | 19.x | DOM rendering |
| `react-router-dom` | v7.x | Client-side routing (file-system routing not needed for SPA) |
| `@tanstack/react-query` | v5.x | Server state — polling, caching, background refresh |
| `zustand` | v5.x | Local UI state (modals, filters, sidebar) |
| `shadcn/ui` (components) | latest | Component library (copied source, not npm dependency) |
| `tailwindcss` | v4.x | CSS utilities (shadcn/ui dependency) |
| `recharts` | v2.x | Analytics charts (line, bar, area — for click/conversion reports) |
| `lucide-react` | latest | Icon set |
| `date-fns` | latest | Date formatting + manipulation |

> **ADR-006**: Vite + React over Next.js — admin panel is an internal tool, no SEO/SSR needed. Static export eliminates Node.js from production. Faster builds, simpler embedding.

### Development

| Package | Version | Purpose |
|---------|---------|---------|
| `vite` | v6.x | Build toolchain + dev server with HMR |
| `typescript` | 5.5+ | Type safety |
| `@vitejs/plugin-react` | latest | React Fast Refresh |
| `eslint` | latest | Linting |
| `prettier` | latest | Code formatting |

---

## Infrastructure

| Service | Technology | Version | Purpose |
|---------|-----------|---------|---------|
| Transactional DB | PostgreSQL | 16-alpine | Campaign/stream/offer config, users, settings |
| Cache + Queue | Valkey | 8-alpine | Entity cache, async click buffer, sessions, hit limits |
| Analytics DB | ClickHouse | 24 | Click log, conversions, aggregated stats |
| Reverse Proxy | Nginx | alpine | TLS termination in production |
| Monitoring | Grafana | latest | Metrics visualization (optional, Phase 7) |
| Container | Docker Compose | v2 | Single `docker compose up` local + production deployment |

---

## Port Assignments (Default)

| Port | Service | Protocol |
|------|---------|---------|
| **8080** | ZAI TDS (HTTP) | HTTP/1.1 |
| 5432 | PostgreSQL | TCP |
| 6379 | Valkey | TCP (RESP3) |
| 9000 | ClickHouse native protocol | TCP |
| 8123 | ClickHouse HTTP interface | HTTP |
| 3000 | Grafana | HTTP |
| 443 | Nginx (TLS) | HTTPS |

---

## Configuration

Loaded from `config.yaml` at startup. All keys overridable via environment variable.

| Config Key | Env Override | Purpose | Required |
|------------|-------------|---------|---------|
| `server.host` | `SERVER_HOST` | Bind address | No (default `0.0.0.0`) |
| `server.port` | `SERVER_PORT` | HTTP port | No (default `8080`) |
| `postgres.dsn` | `DATABASE_URL` | PostgreSQL connection string | **Yes** |
| `valkey.addr` | `VALKEY_URL` | Valkey server address | **Yes** |
| `clickhouse.addr` | `CLICKHOUSE_URL` | ClickHouse native address | **Yes** |
| `geoip.country_db` | — | Path to `GeoLite2-Country.mmdb` | **Yes** |
| `geoip.city_db` | — | Path to `GeoLite2-City.mmdb` | **Yes** |
| `geoip.asn_db` | — | Path to `GeoLite2-ASN.mmdb` | **Yes** |
| `system.salt` | `SYSTEM_SALT` | Password + token hashing salt | **Yes** |
| `system.debug` | `DEBUG` | Enable debug logging + pprof | No |
| `system.log_level` | `LOG_LEVEL` | `debug/info/warn/error` | No (default `info`) |

---

## External Data Files

Loaded into process memory at startup — zero disk I/O on hot path.

| File | Source | Update Frequency | Approx. Size |
|------|--------|-----------------|-------------|
| `GeoLite2-Country.mmdb` | MaxMind (free) | Weekly | ~6 MB |
| `GeoLite2-City.mmdb` | MaxMind (free) | Weekly | ~70 MB |
| `GeoLite2-ASN.mmdb` | MaxMind (free) | Weekly | ~8 MB |
| `IP2Location-LITE-ASN.BIN` | IP2Location (free tier) | Monthly | ~30 MB |

---

## Keitaro → ZAI TDS Migration Notes

| Keitaro | ZAI TDS | Notes |
|---------|---------|-------|
| MySQL / MariaDB | PostgreSQL 16 | Schema migration script needed (Phase 7) |
| ADODB raw SQL | pgx + sqlc | SQL preserved where possible, parametrized |
| PHP Redis ext | go-redis v9 | Same RESP protocol — drop-in |
| MySQL click tables | ClickHouse | 10–100x query speedup at scale |
| PHP 7.2 + RoadRunner | Go 1.23 binary | No PHP, no Node in production |
| IonCube encrypted | MIT licensed | Open source |
| `config.ini.php` | `config.yaml` | Same logical key structure |
| `keitaro_` table prefix | No prefix | Dedicated database |
| `admin_api/v1/` | `/api/v1/` | Route prefix change |

---

## Outdated / Superseded Packages

| Item | Superseded By | Reason |
|------|--------------|--------|
| `reference/legacy-nextjs/` Next.js + Prisma + SQLite | Go + pgx + ClickHouse | ADR-001: SQLite single-writer lock, Node.js single-thread limits, 10–50ms framework overhead |
| Keitaro's ADODB | sqlc + pgx | Parameterized queries, compile-time safety |
| Keitaro's MySQL analytics | ClickHouse | Sub-second aggregation at billions of rows |
| Keitaro's Redis (bare) | Valkey 8 | BSD license, drop-in compatible |
