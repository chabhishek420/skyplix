---
phase: 10-advanced-analytics
plan: 01
type: execute
wave: 1
depends_on:
  - 09-01
files_modified:
  - internal/analytics/service.go
  - internal/analytics/repository.go
  - internal/admin/handler/reports.go
  - internal/server/routes.go
  - test/unit/analytics/service_test.go
autonomous: true
user_setup: []
must_haves:
  truths:
    - Analytics endpoints expose campaign and stream metrics with explicit tenant scope.
    - Cohort and funnel query inputs are validated and bounded.
  artifacts:
    - internal/analytics/service.go
    - internal/admin/handler/reports.go
    - test/unit/analytics/service_test.go
  key_links:
    - tenant_context -> analytics_query_filters -> response payload
---

# Phase 10 Plan 01: Tenant-Scoped Analytics API Foundation

## Objective

- **What:** Build the first tenant-scoped analytics service and API endpoint set.
- **Why:** Advanced analytics requires reusable query primitives before UI/report expansion.
- **Output:** Service + handlers + tests for core campaign/stream metric retrieval.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-advanced-analytics/10-CONTEXT.md

## Tasks

<task type="code">
  <name>Implement analytics service primitives</name>
  <files>internal/analytics/service.go, internal/analytics/repository.go</files>
  <action>Create tenant-scoped query contracts for campaign and stream aggregates, including date-range and grouping inputs.</action>
  <verify>Service-level unit tests validate query shaping and guardrails.</verify>
  <done>Analytics service returns deterministic aggregate structures for valid requests.</done>
</task>

<task type="code">
  <name>Expose analytics handlers and route wiring</name>
  <files>internal/admin/handler/reports.go, internal/server/routes.go</files>
  <action>Add authenticated analytics endpoints that consume tenant context and call the analytics service.</action>
  <verify>Handler tests confirm tenant-scope enforcement and request validation behavior.</verify>
  <done>Endpoints return stable JSON payloads with clear error handling on invalid inputs.</done>
</task>

<task type="test">
  <name>Add unit coverage for analytics service and handler contracts</name>
  <files>test/unit/analytics/service_test.go</files>
  <action>Create table-driven tests for valid aggregates, empty result sets, and invalid filter combinations.</action>
  <verify>go test ./test/unit/... and go test ./internal/admin/... pass.</verify>
  <done>Critical analytics paths are covered by repeatable automated tests.</done>
</task>

## Verification

- `go test ./internal/analytics/...`
- `go test ./internal/admin/...`
- `go test ./test/unit/...`
- `go build ./...`

## Success Criteria

- [ ] Tenant-scoped analytics service is implemented and routable.
- [ ] Handler validation blocks malformed or unbounded analytics queries.
- [ ] Automated tests cover success and failure analytics API flows.
