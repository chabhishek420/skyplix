---
phase: 05-conversion-tracking-analytics
task: 1
total_tasks: 4
status: in_progress
last_updated: 2026-04-03T09:05:09.680Z
---

<current_state>
Phase 5 has been fully replanned with user context. 4 new plans across 3 waves are ready. Execution was started (`/gsd:execute-phase 5`) — Wave 1 (Plan 5.1: ClickHouse MVs) agent was spawned but session was paused before it completed. No execution artifacts have been written to disk yet.
</current_state>

<completed_work>
- **CONTEXT.md captured**: User decisions on stats aggregation (hourly+daily SummingMergeTree MVs), reporting API (single flexible endpoint, multi-level grouping, presets+custom range, rows+totals+meta response), attribution (24h Valkey + CH fallback, multiple conversions per click, append-only rows), postback flexibility (global key only, Keitaro-compatible URL template macros).
- **RESEARCH.md written**: Technical research covering ClickHouse MV patterns, analytics service architecture, Go-side merge strategy, dimension registry, device-only limitations.
- **4 Plans created and verified**:
  - 5.1 (Wave 1): ClickHouse materialized views & stats tables — DDL only, `005_create_stats_materialized_views.sql`
  - 5.2 (Wave 2): Analytics reporting service — `internal/analytics/` (models, query builder, service) + unit tests
  - 5.3 (Wave 3): Reporting API endpoint — `handler/reports.go`, route/server wiring
  - 5.4 (Wave 2): Postback URL templates — `macro/postback.go`, Keitaro-compatible macros
- **Plan checker passed**: All 5 requirement IDs covered, project guidelines honored, DAG acyclic.
- **Old plans removed**: Previous 5.1/5.2/5.3 plans (from prior session) deleted before replan.
- **Prior implementation still in codebase**: Conversion models, attribution service, postback handler, multi-table writer, routes — all from prior sessions — are still in codebase and working.
</completed_work>

<remaining_work>
- **Plan 5.1** (Wave 1): Create `db/clickhouse/migrations/005_create_stats_materialized_views.sql` — 4 SummingMergeTree target tables + 4 insert-triggered MVs (hourly/daily × clicks/conversions). NOT STARTED.
- **Plan 5.2** (Wave 2): Implement `internal/analytics/` package — models.go, query_builder.go, service.go + unit tests. NOT STARTED.
- **Plan 5.3** (Wave 3): Implement `internal/admin/handler/reports.go`, wire routes. NOT STARTED.
- **Plan 5.4** (Wave 2): Implement `internal/macro/postback.go` + affiliate network postback URL endpoint. NOT STARTED.
- **Migration 004**: `db/clickhouse/migrations/004_expand_conversions.sql` still needs applying to running ClickHouse (from prior session).
</remaining_work>

<decisions_made>
- **Replan from scratch**: User chose to discard old 5.1/5.2/5.3 plans and replan with proper context/research.
- **Stats aggregation**: Hourly + daily granularity, SummingMergeTree with insert-triggered MVs, all key dimensions pre-aggregated, full metrics suite.
- **Reporting API**: Single flexible `/api/v1/reports` endpoint, multi-level grouping, presets + custom date range, rows + totals + metadata response.
- **Attribution**: Keep 24h Valkey TTL + CH fallback. Multiple conversions per click allowed. Append-only rows.
- **Postback**: Global key only. Keitaro-compatible URL template macros.
- **Go-side merge**: Click stats and conversion stats queried separately, merged in Go (no ClickHouse JOIN).
- **Device dimension limitation**: Device/OS/browser only in click stats; conversion stats fall back to campaign+stream+offer+country.
</decisions_made>

<blockers>
- None. All plans verified and ready for execution.
</blockers>

<context>
The project is a high-performance TDS (traffic distribution system) with <5ms p99 latency target. Phase 5 closes the tracking loop: clicks are already recorded in ClickHouse — this phase links conversions back to clicks and makes the data queryable via a reporting API. The existing postback handler, attribution service, and multi-table batch writer are already implemented and working. What remains is the analytics/reporting layer: ClickHouse materialized views for pre-aggregation, a Go analytics service for querying and merging stats, an HTTP reporting API, and postback URL template generation.
</context>

<next_action>
Start with: `/gsd:execute-phase 5` — this will pick up all 4 incomplete plans and execute them wave by wave (Wave 1: Plan 5.1, Wave 2: Plans 5.2 + 5.4 in parallel, Wave 3: Plan 5.3).
</next_action>
