---
phase: 12-ml-optimization
plan: 01
type: execute
wave: 1
depends_on:
  - 10-01
  - 11-01
files_modified:
  - internal/pipeline/stage/9_choose_stream.go
  - internal/optimizer/service.go
  - internal/optimizer/features.go
  - internal/admin/handler/campaigns.go
  - test/unit/optimizer/service_test.go
autonomous: true
user_setup: []
must_haves:
  truths:
    - Optimization mode is explicitly toggleable per campaign with deterministic fallback behavior.
    - Optimization decisions are traceable through logged features and selected outcomes.
  artifacts:
    - internal/optimizer/service.go
    - internal/optimizer/features.go
    - test/unit/optimizer/service_test.go
  key_links:
    - campaign.optimization_mode -> optimizer.decision -> stream_selection
---

# Phase 12 Plan 01: Optimization Strategy Bootstrap

## Objective

- **What:** Introduce pluggable optimization strategy wiring with safe fallback controls.
- **Why:** ML optimization must be observable and reversible before model sophistication increases.
- **Output:** Strategy interface + campaign toggle integration + decision trace tests.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ml-optimization/12-CONTEXT.md

## Tasks

<task type="code">
  <name>Implement optimizer strategy interface and feature extraction</name>
  <files>internal/optimizer/service.go, internal/optimizer/features.go</files>
  <action>Create optimizer contracts and feature-building functions from click/campaign context with deterministic defaults.</action>
  <verify>Optimizer unit tests confirm deterministic outputs for fixed inputs.</verify>
  <done>Optimizer service can score/select candidates using explicit feature payloads.</done>
</task>

<task type="code">
  <name>Integrate optimizer toggle into stream selection path</name>
  <files>internal/pipeline/stage/9_choose_stream.go, internal/admin/handler/campaigns.go</files>
  <action>Wire per-campaign optimization mode toggle with safe fallback to existing deterministic stream selection when disabled or unavailable.</action>
  <verify>Pipeline tests validate unchanged legacy behavior when optimization is off.</verify>
  <done>Optimization integration is optional, gated, and non-breaking for existing routing behavior.</done>
</task>

<task type="test">
  <name>Add optimization decision trace tests</name>
  <files>test/unit/optimizer/service_test.go</files>
  <action>Add tests for feature extraction, strategy outputs, and fallback routing behavior under edge conditions.</action>
  <verify>go test ./test/unit/... and go test ./internal/pipeline/stage/... pass.</verify>
  <done>Decision path behavior is covered by tests and supports future model iteration.</done>
</task>

## Verification

- `go test ./internal/optimizer/...`
- `go test ./internal/pipeline/stage/...`
- `go test ./test/unit/...`
- `go build ./...`

## Success Criteria

- [ ] Campaign-level optimization toggle is wired with deterministic fallback.
- [ ] Optimizer decisions are traceable through explicit feature/decision outputs.
- [ ] Unit tests validate optimizer behavior and fallback safety.
