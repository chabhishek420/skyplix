---
phase: 11-webhook-notifications
plan: 01
type: execute
wave: 1
depends_on:
  - 09-01
files_modified:
  - internal/model/models.go
  - internal/worker/webhook_dispatcher.go
  - internal/worker/webhook_queue.go
  - internal/admin/handler/webhooks.go
  - test/unit/worker/webhook_dispatcher_test.go
autonomous: true
user_setup: []
must_haves:
  truths:
    - Tenant webhook endpoints are configurable with secret signing metadata.
    - Delivery pipeline supports retries with observable failure states.
  artifacts:
    - internal/worker/webhook_dispatcher.go
    - internal/admin/handler/webhooks.go
    - test/unit/worker/webhook_dispatcher_test.go
  key_links:
    - conversion_event -> webhook_queue -> signed_http_delivery
---

# Phase 11 Plan 01: Webhook Delivery Core

## Objective

- **What:** Build tenant-configurable webhook delivery with retry and signing support.
- **Why:** Real-time conversion alerts require a reliable outbound event pipeline.
- **Output:** Webhook config API + dispatcher + retry-aware tests.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-webhook-notifications/11-CONTEXT.md

## Tasks

<task type="code">
  <name>Define webhook config and queue contracts</name>
  <files>internal/model/models.go, internal/worker/webhook_queue.go</files>
  <action>Add tenant webhook configuration primitives and queue payload structure for conversion events.</action>
  <verify>Model and queue unit tests validate schema behavior and serialization safety.</verify>
  <done>Webhook config and queue contracts are explicit, versionable, and tenant scoped.</done>
</task>

<task type="code">
  <name>Implement dispatcher with signing and retry behavior</name>
  <files>internal/worker/webhook_dispatcher.go</files>
  <action>Implement delivery worker with HMAC signing, bounded retry policy, and structured error/status reporting.</action>
  <verify>Dispatcher tests cover success, retryable failure, and permanent failure paths.</verify>
  <done>Dispatcher processes events with deterministic retry behavior and emits observable status transitions.</done>
</task>

<task type="code">
  <name>Expose webhook admin handlers</name>
  <files>internal/admin/handler/webhooks.go</files>
  <action>Add CRUD/validation handlers for tenant webhook endpoints and signing secrets metadata.</action>
  <verify>Handler tests validate auth, tenant isolation, and payload validation.</verify>
  <done>Webhook endpoint configuration is manageable through authenticated tenant-aware API routes.</done>
</task>

## Verification

- `go test ./internal/worker/...`
- `go test ./internal/admin/...`
- `go test ./test/unit/...`
- `go build ./...`

## Success Criteria

- [ ] Tenant webhook endpoints can be created and validated through admin APIs.
- [ ] Dispatcher signs and retries outbound webhook deliveries deterministically.
- [ ] Unit tests cover delivery success/failure/retry and configuration validation paths.
