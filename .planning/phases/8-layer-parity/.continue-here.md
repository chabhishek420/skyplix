---
context: phase
phase: 8-layer-parity
task: 4
total_tasks: 5
status: in_progress
last_updated: 2026-04-10T14:06:02.523Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Stale-doc drift — Planning docs/report files contained conflicting completion claims vs code reality — verify from live code/tests before any go/no-go statement.
- [ ] CONSTRAINT: Launch-proof gap — Build/unit pass was insufficient for launch readiness — require end-to-end evidence across all 5 layers before approving launch.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Doc-first verification | Prior reports implied full completion, but placeholders and contradictions were found in planning artifacts. | blocking | Start every verification cycle with code/test/runtime evidence collection before reading summaries. |
| Unit-only launch decision | Compile/unit checks passed but do not prove paid-ad to conversion pipeline behavior. | blocking | Require explicit E2E trace validation (source -> cloaker -> resolver -> tracker -> final conversion) before launch verdict. |
| Ambiguous phase path | Both `8-layer-parity` and `08-layer-parity` exist, causing potential resume drift. | advisory | Treat `8-layer-parity` as canonical until cleanup is performed. |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Paused in Phase 8 verification refresh. The focus shifted from trusting prior summaries to verifying current implementation and launch readiness directly from code and test execution.

Evidence collected this session:
- `_token` / `_subid` URL injection exists in `internal/pipeline/stage/13_generate_token.go`.
- `allow_change_referrer` and `?referrer=` override exist in `internal/config/config.go`, `config.yaml`, and `internal/pipeline/stage/3_build_raw_click.go`.
- Sticky visitor binding exists via Valkey-backed binding service and landing/offer stages in `internal/binding/binding.go`, `internal/pipeline/stage/11_choose_landing.go`, and `internal/pipeline/stage/12_choose_offer.go`.
- Cloaker decision path exists in `internal/pipeline/stage/cloak_decision.go`.

Checks run this session:
- `go build ./...` ✅
- `go test ./...` ✅
- `go test ./internal/pipeline/stage -run "TestGenerateTokenStage|TestGenerateTokenStage_WithRedirectAction|TestGenerateTokenStage_NonRedirectAction|TestGenerateTokenStage_NoStream"` ✅
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Detect active pause context and select destination — Done (`.planning/phases/8-layer-parity/.continue-here.md`).
- Task 2: Gather live implementation evidence for layer parity — Done.
- Task 3: Run build/test verification commands — Done.
- Task 4: Launch-readiness blocker assessment — In progress (core blockers identified and documented).
</completed_work>

<remaining_work>

- Task 4: Finish strict PASS/BLOCK verdict per layer with launch threshold explicitly documented.
- Task 5: Execute or obtain manual E2E evidence for full paid-ad click flow through all 5 layers.
- Validate production safety config before any launch claim (`system.debug=false`, secure `system.salt`, production DSNs/secrets).
- Optional hygiene: resolve `8-layer-parity` vs `08-layer-parity` duplication to prevent resume ambiguity.
</remaining_work>

<decisions_made>

- Decided to distrust outdated `.md` status files and verify from code/tests/runtime state only.
- Chose `8-layer-parity` as canonical handoff path for this pause.
- Chose to block launch readiness until config and E2E evidence are complete, even though build/unit checks pass.
</decisions_made>

<blockers>
- Launch blocker: `config.yaml` currently has `system.debug: true` and placeholder salt (`change-me-in-production-min-32-chars`).
- Launch blocker: No completed end-to-end paid-ad -> cloaker -> resolver -> tracker -> final conversion proof captured in this session.
- Operational blocker: Repo is heavily dirty (`685` changed paths), so release signal is noisy and risky.
- Planning completeness warning: placeholder markers found in `.planning/phases/02-campaign-engine/PHASE.md`.
</blockers>

## Required Reading (in order)
1. `.planning/phases/8-layer-parity/PLAN.md` — phase goals and expected layer-parity scope.
1. `.planning/HANDOFF.json` — structured machine-readable pause state for resume tooling.
1. `config.yaml` — current runtime safety posture (debug/salt/secrets).
1. `internal/pipeline/stage/13_generate_token.go` — resolver layer parameter injection logic.
1. `internal/pipeline/stage/3_build_raw_click.go` — referrer override behavior.
1. `internal/binding/binding.go` — sticky binding persistence semantics.
1. `internal/pipeline/stage/11_choose_landing.go` — landing sticky binding behavior.
1. `internal/pipeline/stage/12_choose_offer.go` — offer sticky binding behavior.
1. `internal/pipeline/stage/cloak_decision.go` — cloaker decision path for bot/researcher traffic.

## Critical Anti-Patterns (do NOT repeat these)
- Assuming launch readiness from legacy summaries -> always derive verdict from current code/tests/runtime checks.
- Treating unit/build green as launch green -> require explicit 5-layer E2E evidence.

## Infrastructure State
- No active `zai-tds` process detected in local process list during pause.
- Local test/build environment is healthy (`go build`, `go test ./...` pass).
- Config points to local infra endpoints (`localhost` Postgres/Valkey/ClickHouse).
- Worktree is heavily dirty with broad unrelated deletions/edits.

<context>
Mental model at pause: implementation appears materially present for layers 1-4 based on code and tests, but launch posture remains blocked due missing production-safety config and lack of full path E2E evidence for the user’s paid-ad flow definition.
</context>

<next_action>
Start with: produce a strict layer-by-layer PASS/BLOCK table from live code evidence, then run/record missing E2E validation and production-config hardening checks before any launch decision.
</next_action>
