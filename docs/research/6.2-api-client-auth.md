---
phase: 6
plan: 2
wave: 1
depends_on: [1]
files_modified:
  - admin-ui/src/lib/api.ts
  - admin-ui/src/query-provider.tsx
  - admin-ui/src/components/auth/login-guard.tsx
autonomous: true

must_haves:
  truths:
    - "API Client supports Token injection via headers"
    - "Unauthenticated users are prompted for an API token"
  artifacts:
    - "admin-ui/src/lib/api.ts"
---

# Plan 6.2: API Client and Auth Guard

<objective>
Establish communication between the React SPA and the Go Admin API using Axios and TanStack Query, plus a token-based Auth Guard.

Purpose: The UI needs to safely authenticate (using API key) prior to fetching or mutating data.
Output: Typed API wrappers, QueryClient setup, and a Login block for token input.
</objective>

<context>
Load for context:
- admin-ui/src/App.tsx
- internal/admin/handler/settings.go (Reference for Auth handling config)
</context>

<tasks>

<task type="auto">
  <name>Setup API Client and Providers</name>
  <files>admin-ui/src/lib/api.ts, admin-ui/src/query-provider.tsx, admin-ui/src/main.tsx</files>
  <action>
    Create an `api.ts` making an Axios instance configured with `baseURL: '/api/v1'`. Add interceptors to attach `Authorization: Bearer {token}` from localStorage. Create a TanStack query provider.
    AVOID: Hardcoding `localhost`. Use relative base URLs so embedding works seamlessly.
  </action>
  <verify>npm run check (TypeScript compiles cleanly)</verify>
  <done>Axios client intercepts inject credentials properly</done>
</task>

<task type="auto">
  <name>Create Login Guard Component</name>
  <files>admin-ui/src/components/auth/login-guard.tsx, admin-ui/src/App.tsx</files>
  <action>
    Create a `LoginGuard` component. If API token is absent or invalid (401 from API), render a premium, centered dark-themed Auth component requesting the API Key. Otherwise, render children.
    This component should wow the user with its glassmorphism/premium design matching the `web_application_development` aesthetic guidelines.
    AVOID: Complex session cookies handling for now; API tokens are sufficient for phase 6 as specified in the Roadmap.
  </action>
  <verify>npm run dev renders the login UI when no token is present</verify>
  <done>User is blocked from the dashboard unless a token is present in local storage</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Users can input an API token.
- [ ] Invalid tokens are wiped and prompt re-login.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
