---
phase: 6
plan: 4
wave: 2
depends_on: [1, 2]
files_modified:
  - admin-ui/src/pages/campaigns/index.tsx
  - admin-ui/src/pages/campaigns/edit.tsx
  - admin-ui/src/components/campaigns/stream-editor.tsx
autonomous: true

must_haves:
  truths:
    - "Users can comprehensively manage Campaigns and their nested Streams"
  artifacts:
    - "admin-ui/src/pages/campaigns/edit.tsx"
---

# Plan 6.4: Campaign & Stream Management

<objective>
Implement the Campaign and Stream administrative interfaces, replicating Keitaro's campaign orchestration capabilities via the `/api/v1/campaigns` endpoints.

Purpose: Campaigns and Streams are the core routing entities of SkyPlix TDS. The UI must provide an intuitive way to manage their complex relationships and filters.
Output: Data tables for Campaigns, and a multi-tab form for Campaign configurations and nested Stream schema editing.
</objective>

<context>
Load for context:
- admin-ui/src/lib/api.ts
- internal/admin/handler/campaigns.go
- internal/admin/handler/streams.go
</context>

<tasks>

<task type="auto">
  <name>Campaign List & Top-Level Form</name>
  <files>admin-ui/src/pages/campaigns/index.tsx, admin-ui/src/pages/campaigns/edit.tsx</files>
  <action>
    Implement a powerful data table (shadcn/ui + tanstack/react-table) to list Campaigns. Add quick actions (Clone, Disable, Delete).
    Create the Edit form handling basic campaign metadata (Name, Cost Type, Cost Value, Bnd Visitors, Postbacks). Use react-hook-form + zod.
    AVOID: Modifying streams in the main form state. Keep Streams isolated in their own tab to avoid massive form payloads.
  </action>
  <verify>npm run lint passes cleanly</verify>
  <done>User can create, list, and edit top-level campaign settings.</done>
</task>

<task type="auto">
  <name>Stream Engine Editor</name>
  <files>admin-ui/src/components/campaigns/stream-editor.tsx, admin-ui/src/pages/campaigns/edit.tsx</files>
  <action>
    Implement a visual interface inside the Campaign Edit view that lists the Campaign's streams.
    Create a detailed modal/sheet for Stream editing that allows defining Action Type, Weight, Status, and importantly: Filters (Bot, Device, Geo, ISP, Proxy).
    AVOID: Simple forms. Provide a UI where users can dynamically add/remove conditions connected by 'AND' (just like Keitaro).
  </action>
  <verify>npm run build complies cleanly</verify>
  <done>Rich UI for managing streams and their associated traffic filters is functional</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Users can edit campaigns and define their complex stream pipelines through the UI.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
